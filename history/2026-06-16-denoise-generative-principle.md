# 2026-06-16 — denoise: the generative principle under the catalogue

A note on a docs-only denoise pass. Not the raw material — the source and the verdict.

## Source

The PI dumped a long career-direction conversation (a separate line of thinking about research
taste, learning progress, and transfer) and asked what, if anything, to do with it — explicitly
*not* to act on its content. Triaged here, then archived.

## Triage — read as a fourth cold read

Read alongside the three independent cold reads behind §2, the conversation is a **fourth** one:
given the bare idea of taste and nothing about this repo, it re-derived ~80% of the existing
design without contact —

- partition / "the agent can't touch the scorer" = the **integrity floor**;
- a compression-derivative / learning-progress signal = the **learning gradient** (§4);
- generalization across problems = the **transfer / port** thesis;
- "how much structure you uncovered" = **coverage**, the meter;
- "the evaluator must not be the thing optimized" = the **dumb-scorer** invariant.

That independent convergence is evidence the spine is right, not new work to do. It is logged the
same way the §2 cold reads are: agreement is the signal.

## The one additive result — folded in

`Value(X) ≈ learning-progress(X) · transfer(X → goal)`, per unit cost, biased toward uncertainty
— the **generative principle the catalogue was flagged as still missing** (`PLAN.md` §5: *"name
the higher generative principle the catalog falls out of"*). Nearly every catalogue habit is one
facet of a single move: *spend your next step where it buys the most learning that will carry, for
what it costs.* Folded into:

- **`DESIGN.md` §1** — named beneath the one-liner as the *reading* of "evaluating your position"
  (the missing middle: definition → principle → habits → worlds). The one-liner is untouched;
  the principle is our reading, never a script for the player (§10, "plant the condition").
- **`DESIGN.md` §4** — the habits reframed as facets of the principle; added the one missing facet,
  **probe where you're least sure** (the exploration term; guards the self-confirming blind spot).
- **`DESIGN.md` §9** — the allocator's *dynamics* (how goals form, how fast attention switches)
  logged as open: grown under selection, not specified.

## Also restored from history (bounded sweep)

The sweep otherwise confirmed the cleanup was right — most "missing" items are §10 shelf items,
correctly deferred. One real restore:

- **The wish channel** (`PLAN.md` §5) — the smith proposes new parts *and, from day one, new
  catalogue habits* as **inert text** a human ratifies; never read back by the loop. Mechanism +
  the day-zero catalogue use written into `DESIGN.md` §5. Distinct from the smith growing its
  *own* taste un-gated (still deferred, §6).

## Parked (shelf — §10 discipline, left in `history/PLAN.md`)

Provisional-mechanisms inventory; transfer thermometers (alien-family audit, planted-bug canary,
leak watch — the port check is already in §3); coach quality-gates / second-round; situations as
a cheap-eval instrument; the smart-spec null. Each is "add when its failure appears." Folding
them into DESIGN now would re-bloat it and undo the cleanup — they wait for the build to force them.
