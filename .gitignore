__pycache__/
*.pyc
*.pyo
.env
outputs/
.pytest_cache/
.venv/
venv/
# Scratch runs go to outputs/ (ignored above). Curated runs are committed under runs/
# (PLAN.md Pass 0: a run you can't inspect didn't happen).
/outputs_smoke.log
